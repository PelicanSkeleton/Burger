<li>
    {{name}}

    <button class="change-devour" data-id="{{id}}" data-newdevoured="{{devour}}">
        {{#if devour}}ATE!{{else}}EAT!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>